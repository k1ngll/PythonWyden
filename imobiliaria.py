#faça um algoritmito para ler dimensoes de um terreno e depois exibir a area do terreno

base = float(input('digite a base do terreno: '))

altura = float(input('digite a altura do terreno : '))

area = base * altura

print('a area e igual :',area)
